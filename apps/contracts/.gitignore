# Compiler files
cache/
out/

# Dependencies are git submodules (see root .gitmodules); run
# `git submodule update --init --recursive` after cloning.

# Broadcast logs
/broadcast/

# Docs (generated by forge doc)
docs/
!docs/api.md

# Dotenv files (use deploy.env.example as a template)
.env
*.env
!*.env.example
