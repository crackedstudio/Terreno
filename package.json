{
  "name": "terreno",
  "version": "0.1.0",
  "description": "Terreno \u2014 a pixel world map on Base where every land pixel is ownable on-chain.",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "type-check": "turbo run type-check",
    "typecheck": "turbo run type-check",
    "test": "turbo run test:coverage"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "turbo": "^2",
    "typescript": "^5.2.2"
  },
  "packageManager": "pnpm@8.10.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
